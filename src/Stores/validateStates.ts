import { atom } from "recoil";

export const orgVal = atom({
    key: 'orgVal',
    default: null as boolean | null,
});

export const licVal = atom({
    key: 'licVal',
    default: null as boolean | null,
});

export const accVal = atom({
    key: 'accVal',
    default: null as boolean | null,
});

export const pwVal = atom({
    key: 'pwVal',
    default: null as boolean | null,
});